import React from 'react';
import RoleBasedNavigator from './RoleBasedNavigator';

// Since we moved the NavigationContainer to App.js, this component is simpler now
const AppNavigator = () => <RoleBasedNavigator />;

export default AppNavigator;
